export type StaticRoutes = TabRoutes;

type TabRoutes =
  | '/index'
  | '/(actions)'
  | '/(actions)/kudos'
  | '/(actions)/rewards'
  | '/(actions)/statistics'
  | '/(actions)/tasks'
  | '/(todo)'
  | '/(map)'
  | '/(profile)';
